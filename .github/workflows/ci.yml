on:
  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch:
name: ci
jobs:
  ci:
    uses: nr1e/github-workflows/.github/workflows/javascript-library-build.yml@develop
    with:
      node-versions: "[18,20,22]"
      publish-node-version: "22"
