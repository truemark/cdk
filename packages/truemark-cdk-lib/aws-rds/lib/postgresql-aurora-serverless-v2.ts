import {ExtendedConstruct} from "../../aws-cdk";

export interface PostgreSQLAuroraServerlessV2Props {

}

export class PostgreSQLAuroraServerlessV2 extends ExtendedConstruct {
  constructor(scope: ExtendedConstruct, id: string, props: PostgreSQLAuroraServerlessV2Props) {
    super(scope, id);

    // Add stuff here

  }
}
